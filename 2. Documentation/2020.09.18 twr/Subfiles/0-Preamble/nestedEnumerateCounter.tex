% Nests enumerate counter for enumerates.

% Requires package: enumitem

\setlist[enumerate,0]{label*=\arabic*.} % For all levels (0), prepend label with the previous level's label.
\setlist[enumerate,1]{label=\arabic*.}  % For first level, do not prepend (as no previous level exists). :D.

% Default:
% 1. Item 1
%    a) Item 1 of first nested list.
% 2. Item 2

% Change:
% 1. Item 1
%    1.1 Item 1 of first nested list includes previous level's label and also numbering.
% 2. Item 2
















